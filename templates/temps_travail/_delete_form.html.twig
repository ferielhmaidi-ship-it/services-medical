<form method="post" action="{{ path('app_temps_travail_delete', {'id': temps_travail.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ temps_travail.id) }}">
    <button class="btn">Delete</button>
</form>
